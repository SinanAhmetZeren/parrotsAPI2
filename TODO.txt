================================================================================
PARROTS API - TODO / SECURITY & HOUSEKEEPING
================================================================================

[ ] Separate test DB credentials from production
    - parrostestdb currently uses same username/password as parrotsdb
    - Should have its own credentials

[ ] Disable SSH root login on server
    - Create a sudo user and disable PermitRootLogin in sshd_config
    - Standard hardening practice

================================================================================

================================================================================
DISPLAY & UI FIXES
================================================================================

[ ] Vehicle title — replace hard-cut substring with proper ellipsis
    - Mobile: use numberOfLines={1} + ellipsizeMode="tail" instead of substring(0,16)
    - Web: use CSS text-overflow: ellipsis instead of substring(0,18)
    - Affects: VoyageCardProfileHorizontal.jsx, ProfilePageVehicleCard.jsx, FavoritesPageVehicleCard.jsx

[ ] Username hard-cut at 30 on mobile is pointless — remove
    - Max is 25 so it never triggers
    - Just render username directly

[ ] Bio on web — add read more / read less like mobile
    - Mobile shows 200 chars then expands — web shows full with scroll
    - Apply same pattern once 500 char limit is in use

[ ] Waypoint description on mobile — add truncation
    - Currently shows full with no limit — could get very long on screen
    - Consider numberOfLines={5} or similar

================================================================================
