PRE-BUILD CHECKLIST (Development Build)
========================================

1. DATABASE CONNECTION
   - Server .env uses DefaultConnection pointing to 127.0.0.1 (local) or 168.119.119.25 (server)?
   - Must confirm server .env uses Host=168.119.119.25 (direct), not 127.0.0.1 (requires SSH tunnel)
   - Without fix: app works only when SSH tunnel is running from local machine

2. GOOGLE MAPS API KEY
   - Exists in parrotsapp/.env but EAS cloud builds don't read local .env
   - Must set via: eas secret:create --scope project --name GOOGLE_MAPS_API_KEY --value "..."
   - Without fix: Google Maps shows "api key not found" error in build

3. SERVER .env (on Hetzner at /root/parrotsAPI2/.env)
   - Verify ConnectionStrings__DefaultConnection points to 168.119.119.25, not 127.0.0.1
   - Verify SMTP credentials match current app password (debw shsz yesc bdcw)
   - Verify Google__ClientId, Google__ClientSecret, Google__AndroidClientId are present
   - Verify TokenKey is present
   - Verify S3 keys are present

4. GOOGLE SIGN-IN
   - androidClientId is not a valid config param for @react-native-google-signin
   - Remove androidClientId from GoogleAuthButton.jsx configure() call
   - Only webClientId (and optionally iosClientId) are valid
   - Without fix: Google login will fail with config error

5. DEPLOYMENT
   - SSH user: deploy (root SSH access is blocked)
   - SSH command: ssh deploy@168.119.119.25
   - App directory: /root/parrotsAPI2
   - To deploy: git pull, then restart the service

6. AFTER FIXES
   - Rebuild: eas build --profile development --platform android
