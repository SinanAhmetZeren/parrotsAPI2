services:
  db:
    image: postgres:16
    container_name: parrots-db
    restart: unless-stopped
    network_mode: host
    environment:
      POSTGRES_PASSWORD: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

  api:
    build: .
    container_name: parrots-api
    restart: unless-stopped
    env_file: .env
    network_mode: host
    volumes:
      - ./Uploads:/app/Uploads
      - ./logs:/app/logs
    depends_on:
      - db

volumes:
  pgdata:
